# helper_functions.py
# Define the functions that your script is importing.

def get_llm_response(prompt: str) -> str:
    """Stub implementation: replace this with a real LLM call."""
    return f"[LLM response for prompt: {prompt}]"


def print_llm_response(response: str) -> None:
    print(response)
